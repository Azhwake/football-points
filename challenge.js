// Good Luck! You got this 💪🏾
// Write your code here.
function calculatePoints(Wins, draws,losses) {
    let totalPoints = (Wins * 3) +  (draws * 2) + (losses * 0);
    console.log(totalPoints)
}
calculatePoints(5,2,3); 
